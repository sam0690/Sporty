/**
 * Landing feature module types.
 */

export interface HeroSection {
    title: string;
    subtitle: string;
    ctaText: string;
    ctaLink: string;
}

export interface FeatureHighlight {
    id: string;
    icon: string;
    title: string;
    description: string;
}
